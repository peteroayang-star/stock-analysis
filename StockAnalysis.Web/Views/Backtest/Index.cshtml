@{
    ViewData["Title"] = "历史回测";
    bool notFound = ViewBag.NotFound == true;
}

<h4 class="mb-1">历史回测</h4>
<p class="text-muted small mb-3">输入股票名称或代码，验证买入信号的胜率和盈亏比。回测结果将自动统计<strong>主升平台信号</strong>的分段胜率（按二波概率 &lt;50 / 50~65 / 65~80 / 80+ 分组）。</p>
<div class="card" style="max-width:520px">
    <div class="card-body">
        <form method="post">
            <div class="mb-3">
                <input name="stock" class="form-control form-control-lg"
                       placeholder="输入股票名称或代码，例如：平安银行 / 000001"
                       value="@ViewBag.Stock" required autofocus />
            </div>

            @if (notFound)
            {
                <div class="mb-3 p-3 rounded" style="background:#fff3cd;border-left:4px solid #ffc107">
                    <div class="fw-bold text-warning-emphasis">未找到「@ViewBag.Stock」的行情数据</div>
                    <div class="small text-muted mt-1">@ViewBag.ErrorMessage</div>
                </div>
            }

            @Html.ValidationSummary(false, "", new { @class = "text-danger small" })
            <button type="submit" class="btn btn-primary btn-lg w-100">开始回测</button>
        </form>
    </div>
</div>
